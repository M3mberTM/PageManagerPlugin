import React from "react";

import "../components/CommonStyles.css"
// TODO Finish the settings component
export const SettingOption = ({children}) => {
    return (
        <div>
            <sp-body>{children}</sp-body>
        </div>
    );
}
